import { Langar } from 'next/font/google'

export const langar = Langar({
  weight: '400', // Langar only comes in regular weight (400)
  subsets: ['latin'],
  display: 'swap',
  variable: '--font-langar',
})